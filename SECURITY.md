# Security

Use opaque, expiring pickup QR tokens; protect kiosk device identities and event signatures; store payment tokens only in provider adapters. Rate-limit public APIs, audit operator actions, and never make payment demo data real credentials.
